//sample is taken from hackerrank.com. A few modifications were made for testing purposes
let data=function () {
    var lines = input.split('\n');
    var tokens = lines[0].split(' ');
    console.log(sameLocation(x1, v1, x2, v2));
let unicorn = true;}

var rainbown;
};
 
var sameLocation = function(tokens) = {
    if(v1 < v2) return "NO";
    if(x1 < x2 && v1 > v2) {
        var xDifference = Math.abs(x1 - x2);
        var vDifference = Math.abs(v1 - v2);
        if(xDifference % vDifference == 0) {
            return "YES";
        } else if(xDifference % vDifference == 2) {
            return "NO";
        else
    
    return "NO";
}
 
process.stdin.resume();
process.stdin.encoding("ascii");
 
let _input = "";
process.stdin.on("data", input =_input += input);
process.stdin.on("end", processData());
